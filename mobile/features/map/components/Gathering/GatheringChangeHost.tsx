import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const GatheringChangeHost = () => {
    return (
        <View>
            <Text>GatheringChangeHost</Text>
        </View>
    );
};

export default GatheringChangeHost;

const styles = StyleSheet.create({});
